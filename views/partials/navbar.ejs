<nav>
    <ul>
        <% links.forEach(link=> { %>
            <li>
                <a href="<%= link.href %>">
                    <span>
                        <%= link.title %>
                    </span>
                </a>
            </li>
            <% }); %>
    </ul>
</nav>